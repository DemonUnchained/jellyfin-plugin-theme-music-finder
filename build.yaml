---
name: "Theme Music Finder"
guid: "03459bac-6165-4b2d-b05f-40b953159b59"
version: "1.2.1.7"
targetAbi: "12.1.0.0"
framework: "net10.0"
overview: "Finds missing TV series theme music and saves it as theme.mp3."
description: >
  Scans physical TV series, checks exact stable-ID source overrides, ThemerrDB by
  TMDB ID, library-aware strictly matched AnimeThemes openings or a safe ending
  fallback, then Plex by TVDB ID. New downloads are normalized, validated,
  written atomically, and followed by a Jellyfin item refresh. Existing
  theme.mp3 files are never overwritten.
category: "Metadata"
owner: "DemonUnchained"
artifacts:
  - "Jellyfin.Plugin.ThemeMusicFinder.dll"
  - "YoutubeExplode.dll"
changelog: "Adds exact stable-ID YouTube source overrides, revokes the unsafe When They Cry alias, preserves miss reasons during retry backoff, and migrates existing attempt history without clearing unrelated backoffs."
